import React from "react";
import {} from "prop-types";
import "./sponsor-banner.scss";

const propTypes = {};
const defaultProps = {};

export const SponsorBanner = (props) => <div className="component sponsor-banner">Sponsor Banner Template</div>;

SponsorBanner.defaultProps = defaultProps;
SponsorBanner.propTypes = propTypes;